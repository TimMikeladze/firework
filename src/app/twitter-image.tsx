/**
 * X and LinkedIn both letterbox to the same 1.91:1 as Open Graph, so the
 * Twitter card is the same image. Re-exported rather than duplicated: one file
 * to change when the card changes.
 */
export { alt, contentType, default, size } from "./opengraph-image";
